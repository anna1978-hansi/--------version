第1轮  
面试官：这个题确实不太好看。  
面试官：它这一步，你写一些……  
面试官：`Promise.all` 的那种？  
面试官：`Promise`……多开？  
面试者：没事没事。

第2轮  
面试官：那你介绍一下你这个，代表（大概）什么意思。  
面试者：我想一下。

第3轮  
面试者：因为它首先，你这个返回的肯定是一个 `Promise`。  
面试者：然后因为你要实现一个，就是前面定义了三个参数。  
面试者：就是说有一个 `result`，是存储你所有 `Promise` 执行完的结果。

第4轮  
面试者：然后 `count` 的话，就是说你现在执行的一个现存数（应为“计数”）。  
面试者：然后 `index` 就是说，你这个是跟 `Promise` 的一个下标一一对应起来的。  
面试者：就是看你执行到哪一个 `Promise` 了。

第5轮  
面试者：然后剩下的结果，就是在这个 `Promise` 下面写了一个……  
面试者：就是我先定义了一个，是 `add` 的方式。  
面试者：就是 `addData` 吧。  
面试者：就是说你每次添加完 data 的时候，你就会去对它长度进行一个判断。  
面试者：看它是不是所有的 `Promise` 都执行完了。  
面试者：就可以返回成功的结果。

第6轮  
面试者：然后就是，相当于是你第一个 `Promise` 进来的时候，  
面试者：它也是会……就是你跑到 `run` 这个地方去了。  
面试者：它每次 `run` 的时候，它就会去进行一个“你是否能执行这个任务”的比较。

第7轮  
面试者：这个比较条件就是，它现在并发数量是小于你这个 `limit` 的数量，  
面试者：以及你现在……  
面试者：你现在就是你要找到这个执行的 `Promise`，  
面试者：就是这个 `index` 的下标，小于你总体的 `Promise` 的下标（应为“总数”）。  
面试者：相当于是你还没有执行完全部的 `Promise`。

第8轮  
面试者：然后这个时候，就看一下，  
面试者：这个时候你就会把这个 `Promise` 找出来，  
面试者：然后记录一下下标，  
面试者：是因为你下面添加数据的时候要用到。  
面试者：不然的话你的下标，这个时候下标就增加，  
面试者：最像是（待确认）你可以……这个就是要找下一个 `Promise` 了。

第9轮  
面试者：然后把 `count` 加一下，  
面试者：就说明你现在这个数量，已经有一个并发被占用了。  
面试者：然后接下来就是去执行你的一个 `Promise`。

第10轮  
面试者：就是执行成功的话，就是调上面的 `addData` 的函数，  
面试者：把成功结果返回。  
面试者：然后它这一方，就会去进行一个数量的判断。  
面试者：如果你整体的数量到了就 OK。  
面试者：然后没有的话，就相当于不做操作。  
面试者：然后就继续。

第11轮  
面试者：然后失败的话，就直接调用你的这个 `reject`。  
面试者：这个 `all`（原文“out”，推测为 `all`，待确认）就……  
面试者：因为它必须要所有的（“困扣”待确认，疑似“成功”或“fulfilled”）嘛。

第12轮  
面试者：然后无论你最后的结果是怎么样的，  
面试者：你都需要把你的一个数量，  
面试者：就是你正在执行的数量减减。